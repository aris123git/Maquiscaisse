#!/bin/bash
# Script post-merge — projet Android natif (Kotlin + Room + Hilt).
# Les migrations Room s'appliquent automatiquement au lancement de l'app.
# Aucune dépendance npm ni commande serveur à relancer côté Replit.
set -e
echo "Post-merge OK — rien à faire pour un projet Android pur."
